# @img/sharp-* native packages declare engines.yarn >=3; this project uses Yarn Classic.
# Without this, optional sharp binaries are skipped and the bot crashes at runtime on Linux.
ignore-engines true
# Allow installing on Windows even though sharp binaries are Linux-only.
ignore-platform true
